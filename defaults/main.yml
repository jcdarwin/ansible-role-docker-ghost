---
# defaults file for mebooks.ansible-role-docker-ghost

ghost:
  owner: admin
  owner_password: WHATEVER
  domain: domain.tld
  source: domain.tld
  install_dir: /etc/ghost
  remote: git@github.com:whoever/blog.git
  mail:
    transport: SMTP # or SES
    from: '"Blog" <noreply@blog.domain.tld>'
    smtp_service: Mailgun # or ~
    user: postmaster@blog.domain.tld
    pass: password
